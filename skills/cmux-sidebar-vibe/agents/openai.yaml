interface:
  display_name: "cmux Sidebar Vibe"
  short_description: "Build left-sidebar custom views with cmux's runtime SwiftUI-style interpreter."
  default_prompt: "Use this skill to create, inspect, or revise vibe-coded cmux left-sidebar custom sidebars in ~/.config/cmux/sidebars using the runtime Swift interpreter and current cmux docs/source entrypoints."
